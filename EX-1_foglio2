def ContaParola (file, parola):
    parola = "ciao"
    file = open('file.txt', 'r')
    count = 0
    for line in file:
        if parola in line:
            count = count + 1
    print (count)
    return count
