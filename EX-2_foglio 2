def ContaParole (doc):
    with open (doc, 'r') as file:
    lista = []
    contenuto = file.read()
    for word in file:
        count = contenuto.count(word)
        lista.append (word)
        lista.append (count)
    print(lista)